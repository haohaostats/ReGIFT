The frozen-r4.rds regression reference was captured from the unmodified
0.0.1.9007 namespace plus the 2026-09-09 state-anchor uncertainty adapter.
It contains deterministic, artificial software-test inputs for paired and
unpaired designs and their numerical outputs, plus a primary-API reference
using the base-model one-sweep penalty probe. No patient records are included.
The reference tests compare calibrated predictions, anchors, objective traces,
shared/nuisance products and shrinkage weights with a numerical tolerance.
See inst/FROZEN_MODEL.json for the frozen source hashes.
